% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cover_page.R
\name{pg_build_cover}
\alias{pg_build_cover}
\title{Build an HTML Cover Page for a Researcher Profile}
\usage{
pg_build_cover(
  author_name,
  affiliation,
  photo_path = NULL,
  orcid = NULL,
  linkedin = NULL,
  website = NULL,
  stats,
  theme_color = "#1B4F72",
  lang = "fr"
)
}
\arguments{
\item{author_name}{Character(1). Full name of the researcher.}

\item{affiliation}{Character(1). Institutional affiliation displayed below
the author name.}

\item{photo_path}{Character(1) or \code{NULL}. Path to a local photo file
(JPEG or PNG). When \code{NULL} (default), a circular initials placeholder is
generated from the first letters of \code{author_name}.}

\item{orcid}{Character(1) or \code{NULL}. ORCID identifier (e.g.
\code{"0000-0001-2345-6789"}). Displayed as a clickable link when provided.}

\item{linkedin}{Character(1) or \code{NULL}. LinkedIn profile URL.}

\item{website}{Character(1) or \code{NULL}. Personal or institutional website
URL. When provided a QR code is generated using \code{\link[qrcode:qr_code]{qrcode::qr_code()}} and
embedded as a base64 PNG image.}

\item{stats}{A named list with exactly six numeric elements:
\code{Articles}, \code{Books}, \code{Seminars}, \code{Theses}, \code{Awards}, \code{Projects}.
Each value is displayed as a large bold number in the stats grid.}

\item{theme_color}{Character(1). Hex colour code used for the left accent
band gradient, headings, and link colours (default \code{"#1B4F72"}).}

\item{lang}{Character(1). Display language: \code{"fr"} (default) or \code{"en"}.
Controls the labels in the stats grid and footer text.}
}
\value{
A \code{character(1)} HTML string suitable for inclusion in R Markdown
or Quarto documents via \code{\link[htmltools:HTML]{htmltools::HTML()}}.
}
\description{
Generates a self-contained HTML cover page featuring the researcher's photo
(or initials placeholder), affiliation, social links (ORCID, LinkedIn),
a QR code for their website, and a 3x2 statistics grid summarising their
scientific output. The design uses CSS Grid with a two-column layout
(25 \% left accent band / 75 \% content area) and the Lato font loaded via
Google Fonts inline CSS.
}
\details{
\subsection{Design}{

The cover uses a \strong{CSS Grid} two-column layout:
\itemize{
\item \strong{Left band (25 \%)}: a vertical gradient from \code{theme_color} to a darker
shade (computed via \code{\link[colorspace:lighten]{colorspace::darken()}}).
\item \strong{Right area (75 \%)}: contains the author photo (clipped to a circle
with a 4 px white border), name, affiliation, social-link icons, the
stats grid (3 columns x 2 rows), an optional QR code, and a footer.
}
}

\subsection{Security}{

All user-supplied text values are interpolated with \code{\link[glue:glue_safe]{glue::glue_safe()}} to
prevent XSS injection in the generated HTML. The photo is embedded as a
base64 data URI to keep the output self-contained.
}
}
\examples{
\dontrun{
cover_html <- pg_build_cover(
  author_name = "Marie Dupont",
  affiliation = "Universite de Paris",
  photo_path  = "photo.jpg",
  orcid       = "0000-0001-2345-6789",
  linkedin    = "https://linkedin.com/in/mariedupont",
  website     = "https://mariedupont.fr",
  stats       = list(
    Articles = 24, Books = 3, Seminars = 15,
    Theses = 8, Awards = 2, Projects = 5
  ),
  theme_color = "#1B4F72",
  lang        = "fr"
)
htmltools::browsable(htmltools::HTML(cover_html))
}

}
