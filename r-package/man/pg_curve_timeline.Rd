% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_global.R
\name{pg_curve_timeline}
\alias{pg_curve_timeline}
\title{Curve Timeline of Annual Productions}
\usage{
pg_curve_timeline(data, theme_color = "#1B4F72")
}
\arguments{
\item{data}{A data frame (typically from \code{\link[=pg_classify]{pg_classify()}}) containing at
least the columns \code{type_classified} and \code{year}.}

\item{theme_color}{Character. Hex colour used for the total area and line
(default \code{"#1B4F72"}).}
}
\value{
A \code{ggplot} object.
}
\description{
Plots a multi-layer timeline of research productions over time. The total
annual count is shown as a filled area with a solid line overlay, while
individual production types are drawn as dashed lines coloured by category.
The most productive year is annotated with a rich-text label via
\code{\link[ggtext:geom_richtext]{ggtext::geom_richtext()}}.
}
\examples{
\dontrun{
bib <- pg_read_bib("references.bib")
all <- pg_merge_inputs(bib) |> pg_classify()
pg_curve_timeline(all)
}

}
