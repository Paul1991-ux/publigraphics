% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_inputs.R
\name{pg_merge_inputs}
\alias{pg_merge_inputs}
\title{Merge and deduplicate bibliographic and supplementary data}
\usage{
pg_merge_inputs(bib_data, extra_data = NULL)
}
\arguments{
\item{bib_data}{A tibble produced by \code{\link[=pg_read_bib]{pg_read_bib()}}.}

\item{extra_data}{A tibble produced by \code{\link[=pg_read_extra]{pg_read_extra()}}, or \code{NULL}
(default) if no supplementary data are available.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with the 19-column publigraphics schema.
Rows are ordered by \code{year} (descending) then \code{title} (ascending).
}
\description{
Combines data from \code{\link[=pg_read_bib]{pg_read_bib()}} and optionally \code{\link[=pg_read_extra]{pg_read_extra()}}
into a single standardised tibble. Duplicates are detected by
case-insensitive, whitespace-trimmed title comparison; the first
occurrence (from \code{bib_data}) is retained.
}
\details{
Deduplication compares the normalised title (\code{tolower(str_trim(title))})
across both data sets. When a title appears in both \code{bib_data} and
\code{extra_data}, only the \code{bib_data} row is kept because bibliographic
sources generally carry richer metadata.
}
\examples{
bib_path   <- system.file("extdata", "demo_refs.bib",
                          package = "publigraphics")
extra_path <- system.file("extdata", "demo_extra.csv",
                          package = "publigraphics")
if (nzchar(bib_path) && nzchar(extra_path)) {
  bib_data   <- pg_read_bib(bib_path)
  extra_data <- pg_read_extra(extra_path)
  merged     <- pg_merge_inputs(bib_data, extra_data)
  print(merged)
}

}
