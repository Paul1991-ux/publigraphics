% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_inputs.R
\name{pg_read_bib}
\alias{pg_read_bib}
\title{Read a bibliographic file into a standardised tibble}
\usage{
pg_read_bib(path, format = "auto", encoding = "UTF-8")
}
\arguments{
\item{path}{Character. Path to the bibliography file.}

\item{format}{Character. File format: \code{"auto"} (default), \code{"bib"},
\code{"ris"}, \code{"csv"}, or \code{"xlsx"}.}

\item{encoding}{Character. File encoding (default \code{"UTF-8"}).}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with the 19-column publigraphics schema.
Column \code{source} is set to \code{"bib"} for all rows.
}
\description{
Parses BibTeX (\code{.bib}), RIS (\code{.ris}), CSV (\code{.csv}), or Excel (\code{.xlsx})
files and maps their fields to the canonical publigraphics data schema.
Format is auto-detected from the file extension unless overridden with
the \code{format} argument.
}
\details{
\strong{BibTeX} files are parsed with \code{\link[bib2df:bib2df]{bib2df::bib2df()}}.
The \code{BIBTEXKEY} column is used as \code{pg_id}; the \code{CATEGORY} column becomes
\code{type_raw} (prefixed with \code{@} if not already present). Author fields are
normalised via the internal helper \code{pg_clean_authors()}.

\strong{CSV} files are expected to contain columns whose names match (case
insensitively) the schema field names. Unrecognised columns are silently
dropped and missing columns default to \code{NA}.

\strong{XLSX} files are read with \code{\link[readxl:read_excel]{readxl::read_excel()}} using the same
column-matching logic as CSV files.

\strong{RIS} format support is planned; currently raises an informative error.
}
\examples{
# Using the bundled demo BibTeX file
bib_path <- system.file("extdata", "demo_refs.bib", package = "publigraphics")
if (nzchar(bib_path)) {
  refs <- pg_read_bib(bib_path)
  print(refs)
}

}
