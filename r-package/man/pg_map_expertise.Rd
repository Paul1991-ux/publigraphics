% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_other.R
\name{pg_map_expertise}
\alias{pg_map_expertise}
\title{Geographic Map of Expertise and Consulting Missions}
\usage{
pg_map_expertise(data, theme_color = "#1B4F72")
}
\arguments{
\item{data}{A data frame containing at least \code{type_classified}, \code{title},
\code{city}, and \code{country} columns.}

\item{theme_color}{Character. Base hex colour (default \code{"#1B4F72"}). The
map markers use the complementary colour.}
}
\value{
A named list with two elements:
\describe{
\item{interactive}{A \code{\link[leaflet:leaflet]{leaflet::leaflet()}} map widget, or \code{NULL} on
failure.}
\item{static}{A \code{ggplot} object using \code{\link[rnaturalearth:ne_countries]{rnaturalearth::ne_countries()}}
as the basemap, or \code{NULL} on failure.}
}
}
\description{
Produces both an interactive Leaflet map and a static \code{ggplot2} map showing
the geographic locations of expertise and consulting missions. Uses square
markers (\code{pch = 15}) and a complementary colour derived from the base theme
colour via the \code{colorspace} package.
}
\details{
Locations are geocoded from the \code{city} and \code{country} columns using
\code{\link[tidygeocoder:geocode]{tidygeocoder::geocode()}}. Previously geocoded results are cached within
the returned data to avoid redundant API calls.

This function follows the same logic as \code{pg_map_seminars()} but uses
square markers instead of circles and a complementary colour scheme.
}
\examples{
\dontrun{
bib   <- pg_read_bib("references.bib")
extra <- pg_read_extra("extra.csv")
all   <- pg_merge_inputs(bib, extra) |> pg_classify()
maps  <- pg_map_expertise(all)
maps$interactive   # interactive Leaflet
print(maps$static) # static ggplot
}

}
