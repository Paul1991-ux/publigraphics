% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_other.R
\name{pg_media_summary}
\alias{pg_media_summary}
\title{Horizontal Bar Chart of Media Appearances}
\usage{
pg_media_summary(data, theme_color = "#1B4F72")
}
\arguments{
\item{data}{A data frame containing at least \code{type_classified} and
\code{institution} (or \code{journal_or_venue}) columns. The \code{type} or \code{type_raw}
column is used for sub-type grouping when available.}

\item{theme_color}{Character. Base hex colour for the palette
(default \code{"#1B4F72"}).}
}
\value{
A \code{ggplot} object.
}
\description{
Produces a horizontal grouped bar chart summarising media appearances
(podcasts, interviews, press articles, etc.) by institution or media
outlet. Bars are grouped by sub-type when a \code{type} or \code{type_raw} column
provides finer-grained categorisation (e.g. \code{"podcast"}, \code{"interview"},
\code{"media"}).
}
\examples{
\dontrun{
bib   <- pg_read_bib("references.bib")
extra <- pg_read_extra("extra.csv")
all   <- pg_merge_inputs(bib, extra) |> pg_classify()
pg_media_summary(all)
}

}
