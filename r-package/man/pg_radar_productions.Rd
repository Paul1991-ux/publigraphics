% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_global.R
\name{pg_radar_productions}
\alias{pg_radar_productions}
\title{Radar Chart of Research Productions by Type}
\usage{
pg_radar_productions(data, theme_color = "#1B4F72")
}
\arguments{
\item{data}{A data frame (typically from \code{\link[=pg_classify]{pg_classify()}}) containing at
least the columns \code{type_classified} and \code{year}.}

\item{theme_color}{Character. Hex colour used for the polygon fill and
border (default \code{"#1B4F72"}).}
}
\value{
A \code{ggplot} object with \code{coord_polar()}.
}
\description{
Builds a polar (radar) chart showing the distribution of research
productions across classified types. Each axis represents a production
type and its radial extent is proportional to the count normalised by the
most frequent type (score 0--100). Raw counts are annotated next to each
vertex.
}
\details{
Internally calls \code{\link[=pg_summary_table]{pg_summary_table()}} to obtain per-type counts, then
normalises them to a 0--100 scale for the polar geometry.
}
\examples{
\dontrun{
bib <- pg_read_bib("references.bib")
all <- pg_merge_inputs(bib) |> pg_classify()
pg_radar_productions(all)
}

}
