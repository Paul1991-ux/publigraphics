% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_books.R
\name{pg_wordcloud_books}
\alias{pg_wordcloud_books}
\title{TF-IDF Weighted Word Cloud for Books and Book Chapters}
\usage{
pg_wordcloud_books(
  data,
  theme_color = "#1B4F72",
  lang = "both",
  max_words = 60L
)
}
\arguments{
\item{data}{A data frame (typically from \code{\link[=pg_read_bib]{pg_read_bib()}} followed by
\code{\link[=pg_classify]{pg_classify()}}) containing at least \code{type_classified} and \code{title}
columns. Optionally includes a \code{subtitle} column.}

\item{theme_color}{Character. Base hex colour for the palette
(default \code{"#1B4F72"}).}

\item{lang}{Character. Stopword languages to remove. One of \code{"both"}
(French + English, default), \code{"fr"}, or \code{"en"}.}

\item{max_words}{Integer. Maximum number of words in the cloud
(default \code{60L}).}
}
\value{
A named list with two elements:
\describe{
\item{widget}{An interactive \code{htmlwidget} from \code{\link[wordcloud2:wordcloud2]{wordcloud2::wordcloud2()}}.}
\item{plot}{A static \code{ggplot} object from \code{\link[ggwordcloud:geom_text_wordcloud]{ggwordcloud::geom_text_wordcloud()}}.}
}
}
\description{
Builds a word cloud from the titles and subtitles of books and book chapters.
Words are sized by mean TF-IDF weight. Unlike \code{\link[=pg_wordcloud_articles]{pg_wordcloud_articles()}},
no LDA topic modelling is performed because the document count is typically
too small; instead, words are coloured by a sequential palette derived from
\code{theme_color}.
}
\examples{
\dontrun{
bib <- pg_read_bib("references.bib") |> pg_classify()
wc  <- pg_wordcloud_books(bib)
wc$widget          # interactive
print(wc$plot)     # static PDF-ready
}

}
