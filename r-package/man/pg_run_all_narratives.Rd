% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_narratives.R
\name{pg_run_all_narratives}
\alias{pg_run_all_narratives}
\title{Generate AI Narrative Summaries for All Productions}
\usage{
pg_run_all_narratives(
  data,
  api_key,
  lang = "fr",
  cache_dir = NULL,
  types_to_process = NULL,
  max_items = Inf
)
}
\arguments{
\item{data}{A tibble produced by \code{\link[=pg_classify]{pg_classify()}}. Must contain at least the
columns \code{pg_id}, \code{type_classified}, \code{title}, and \code{abstract}.}

\item{api_key}{Character. Anthropic API key. \strong{Never logged or printed.}}

\item{lang}{Character. Language for the narratives: \code{"fr"} (default) or
\code{"en"}.}

\item{cache_dir}{Character or \code{NULL}. Path to a directory used for caching
narrative results as \code{.rds} files. If \code{NULL} (the default) no caching is
performed. When a cache directory is provided, each result is keyed by
\code{digest::digest(list(pg_id, abstract, lang))}. Requires the \pkg{digest}
package (listed in \code{Suggests}).}

\item{types_to_process}{Character vector. Production types to process.
Defaults to \code{c("article", "seminar", "book")}. Rows whose
\code{type_classified} is not in this vector are silently skipped.}

\item{max_items}{Numeric. Maximum number of items to process (default
\code{Inf}, meaning all eligible rows). Useful for testing or quota management.}
}
\value{
The input \code{data} tibble augmented with six new columns:
\describe{
\item{narrative_problematique}{Character. Research question / central theme.}
\item{narrative_pertinence}{Character. Importance and context.}
\item{narrative_resultat}{Character. Main findings / contribution.}
\item{narrative_question_ouverte}{Character. Open question for reflection.}
\item{narrative_lang}{Character. Language code used for generation.}
\item{narrative_cached}{Logical. \code{TRUE} if the result was loaded from cache.}
}
Rows that were not processed (wrong type or beyond \code{max_items}) receive
\code{NA_character_} for the narrative columns.
}
\description{
Iterates over rows of a classified dataset and dispatches each item to the
appropriate narrative generator (\code{\link[=pg_narrative_article]{pg_narrative_article()}} for articles,
\code{\link[=pg_narrative_seminar]{pg_narrative_seminar()}} for seminars). Results are merged back into the
input tibble as new columns. Supports optional disk caching (via the
\pkg{digest} package) to avoid redundant API calls, built-in rate limiting,
and a CLI progress bar.
}
\details{
\subsection{Dispatch rules}{\tabular{lll}{
   \code{type_classified} \tab Generator called \tab Mapping to output columns \cr
   \code{article} \tab \code{\link[=pg_narrative_article]{pg_narrative_article()}} \tab Direct mapping (same 4 keys) \cr
   \code{book} \tab \code{\link[=pg_narrative_article]{pg_narrative_article()}} \tab Treated as article (title+abstract) \cr
   \code{seminar} \tab \code{\link[=pg_narrative_seminar]{pg_narrative_seminar()}} \tab Mapped from seminar-specific keys \cr
}


For seminars, the four seminar-specific keys (\code{question_centrale},
\code{audience_ciblee}, \code{positionnement_debat}, \code{apport_original}) are mapped
onto the canonical output columns (\code{narrative_problematique},
\code{narrative_pertinence}, \code{narrative_resultat},
\code{narrative_question_ouverte}).
}

\subsection{Caching}{

When \code{cache_dir} is provided, the function creates the directory if it does
not exist, then for each item computes a cache key with
\code{digest::digest(list(pg_id, abstract, lang))}. If a matching \code{.rds} file
exists it is loaded directly; otherwise the API is called and the result
saved.
}

\subsection{Rate limiting}{

A 0.5-second pause (\code{\link[=Sys.sleep]{Sys.sleep()}}) is inserted between consecutive API
calls to respect Anthropic rate limits.
}
}
\examples{
\dontrun{
library(publigraphics)
data <- pg_read_bib("refs.bib") |> pg_classify()
enriched <- pg_run_all_narratives(
  data,
  api_key   = Sys.getenv("ANTHROPIC_API_KEY"),
  lang      = "fr",
  cache_dir = "narrative_cache",
  max_items = 5
)
enriched |> dplyr::select(title, narrative_problematique)
}

}
