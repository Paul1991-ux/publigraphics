% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_books.R
\name{pg_network_coauthors}
\alias{pg_network_coauthors}
\title{Co-authorship Network Graph}
\usage{
pg_network_coauthors(data, author_name, theme_color = "#1B4F72")
}
\arguments{
\item{data}{A data frame (typically from \code{\link[=pg_read_bib]{pg_read_bib()}} followed by
\code{\link[=pg_classify]{pg_classify()}}) containing at least \code{type_classified}, \code{title}, and
\code{authors} columns. The \code{authors} column should be a list-column of
character vectors (one vector per row).}

\item{author_name}{Character. The full name of the focal author as it
appears in the \code{authors} field (e.g., \code{"Dupont, Jean"}).}

\item{theme_color}{Character. Base hex colour for the theme
(default \code{"#1B4F72"}).}
}
\value{
A \code{ggplot} object (from \link[ggraph:ggraph]{ggraph::ggraph}).
}
\description{
Builds an interactive-style co-authorship network centred on a focal author.
Edges connect the focal author to each co-author; edge width encodes the
number of co-publications and edge colour encodes the production type.
Layout uses the Fruchterman--Reingold algorithm (seed 2024).
}
\examples{
\dontrun{
bib <- pg_read_bib("references.bib") |> pg_classify()
pg_network_coauthors(bib, author_name = "Dupont, Jean")
}

}
