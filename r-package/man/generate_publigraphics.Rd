% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_notebook.R
\name{generate_publigraphics}
\alias{generate_publigraphics}
\title{Generate a Complete PubliGraphics Notebook}
\usage{
generate_publigraphics(
  author_name,
  bib_file,
  extra_data = NULL,
  photo = NULL,
  affiliation = "",
  orcid = NULL,
  linkedin = NULL,
  website = NULL,
  api_key_claude = Sys.getenv("ANTHROPIC_API_KEY"),
  theme_color = "#1B4F72",
  output_formats = c("pdf", "html"),
  output_dir = file.path(getwd(), "publigraphics_output"),
  language = "fr",
  use_cache = TRUE,
  open_after = TRUE,
  n_narrative_max = 20L
)
}
\arguments{
\item{author_name}{Character(1). Full name of the researcher (e.g.
\code{"Dupont, Marie"}). Used in the cover page, co-authorship network, and
output file names.}

\item{bib_file}{Character(1). Path to the bibliography file (\code{.bib}, \code{.csv},
or \code{.xlsx}). Parsed by \code{\link[=pg_read_bib]{pg_read_bib()}}.}

\item{extra_data}{Character(1) or \code{NULL}. Path to a supplementary CSV file
containing non-bibliographic outputs (seminars, projects, theses, awards).
Parsed by \code{\link[=pg_read_extra]{pg_read_extra()}}. Default \code{NULL}.}

\item{photo}{Character(1) or \code{NULL}. Path to a local photo file (JPEG/PNG)
for the cover page. Default \code{NULL} (initials placeholder used).}

\item{affiliation}{Character(1). Institutional affiliation for the cover page.
Default \code{""}.}

\item{orcid}{Character(1) or \code{NULL}. ORCID identifier. Default \code{NULL}.}

\item{linkedin}{Character(1) or \code{NULL}. LinkedIn profile URL. Default \code{NULL}.}

\item{website}{Character(1) or \code{NULL}. Personal website URL (also used to
generate a QR code on the cover). Default \code{NULL}.}

\item{api_key_claude}{Character(1). Anthropic API key for narrative generation.
Defaults to \code{Sys.getenv("ANTHROPIC_API_KEY")}. When empty or invalid,
narrative summaries are silently skipped.}

\item{theme_color}{Character(1). Hex colour code for the visual theme
(default \code{"#1B4F72"}). Must be a valid \verb{#RRGGBB} or \verb{#RGB} string.}

\item{output_formats}{Character vector. One or both of \code{"pdf"} and \code{"html"}.
Default \code{c("pdf", "html")}.}

\item{output_dir}{Character(1). Directory where output files and
visualisations are saved. A \verb{viz/} sub-directory is created automatically.
Default \code{file.path(getwd(), "publigraphics_output")}.}

\item{language}{Character(1). Language for bilingual labels and narrative
prompts: \code{"fr"} (default) or \code{"en"}.}

\item{use_cache}{Logical(1). If \code{TRUE} (default), geocoding results and
API responses are cached to avoid redundant calls on re-runs.}

\item{open_after}{Logical(1). If \code{TRUE} (default), the HTML output is
opened in the default browser after generation.}

\item{n_narrative_max}{Integer(1). Maximum number of items to generate
narrative summaries for (default \code{20L}). Limits API usage.}
}
\value{
Invisible named list with elements:
\describe{
\item{pdf_path}{Character or \code{NULL}. Path to the generated PDF file.}
\item{html_path}{Character or \code{NULL}. Path to the generated HTML file.}
\item{data}{The merged and classified tibble used for all outputs.}
\item{duration_seconds}{Numeric. Wall-clock duration of the pipeline.}
}
}
\description{
End-to-end pipeline that reads bibliographic and supplementary data, classifies
research outputs, computes summary statistics, generates AI narrative summaries
(when an Anthropic API key is available), produces all visualisations, builds an
HTML cover page, renders an R Markdown template to HTML, and optionally converts
to PDF via \code{\link[pagedown:chrome_print]{pagedown::chrome_print()}}. The final output files are named with
the author name and current date, and can be opened automatically in a browser.
}
\details{
\subsection{Pipeline Steps}{
\enumerate{
\item \strong{Validate inputs}: file existence, hex colour format, output formats,
language code.
\item \strong{Create directories}: \code{output_dir} and \code{output_dir/viz}.
\item \strong{Parse data}: \code{\link[=pg_read_bib]{pg_read_bib()}}, optionally \code{\link[=pg_read_extra]{pg_read_extra()}}, then
\code{\link[=pg_merge_inputs]{pg_merge_inputs()}} and \code{\link[=pg_classify]{pg_classify()}}.
\item \strong{Compute stats}: \code{\link[=pg_stats_banner]{pg_stats_banner()}} for the cover page and summary.
\item \strong{Narrative summaries} (if API key valid): \code{\link[=pg_run_all_narratives]{pg_run_all_narratives()}}
generates structured AI summaries for up to \code{n_narrative_max} items.
\item \strong{Visualisations}: all \verb{pg_wordcloud_*}, \verb{pg_timeline_*},
\verb{pg_network_*}, \verb{pg_map_*}, \verb{pg_gallery_*}, \verb{pg_gantt_*},
\verb{pg_infographic_*}, and \verb{pg_media_*} functions are called. Each plot is
saved as a 300 dpi PNG via \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.
\item \strong{Cover page}: \code{\link[=pg_build_cover]{pg_build_cover()}} generates the HTML cover.
\item \strong{Template}: the Rmd template is copied from
\code{system.file("templates", package = "publigraphics")} to \code{output_dir}.
\item \strong{Render}: \code{\link[rmarkdown:render]{rmarkdown::render()}} produces the HTML notebook, passing
all params.
\item \strong{PDF} (if requested): \code{\link[pagedown:chrome_print]{pagedown::chrome_print()}} converts the HTML
to PDF.
\item \strong{Rename}: output files are renamed to
\verb{publigraphics_<AuthorName>_<YYYYMMDD>.<ext>}.
\item \strong{Summary}: a \link[cli:cli]{cli::cli} summary is printed with file paths and counts.
\item \strong{Open}: if \code{open_after = TRUE}, the HTML file is opened in the
browser via \code{\link[utils:browseURL]{utils::browseURL()}}.
}
}
}
\examples{
\dontrun{
generate_publigraphics(
  author_name = "Wambo, Paul",
  bib_file    = "references.bib",
  extra_data  = "extra_outputs.csv",
  photo       = "photo.jpg",
  affiliation = "Universite de Yaounde II",
  orcid       = "0009-0005-6062-9227",
  website     = "https://paulwambo.com",
  theme_color = "#1B4F72",
  output_formats = c("pdf", "html"),
  language    = "fr"
)
}

}
