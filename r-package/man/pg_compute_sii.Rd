% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scientific_index.R
\name{pg_compute_sii}
\alias{pg_compute_sii}
\title{Compute the Scientific Influence Index (SII)}
\usage{
pg_compute_sii(
  data,
  focal_author = NA_character_,
  weights = c(PIS = 0.3, BIS = 0.1, KDS = 0.15, SS = 0.1, PFS = 0.15, RS = 0.2)
)
}
\arguments{
\item{data}{A classified tibble from \code{\link[=pg_classify]{pg_classify()}}, ideally already
processed through individual scoring functions.}

\item{focal_author}{Character. Name of the researcher being profiled.}

\item{weights}{Named numeric vector of dimension weights (must sum to 1).
Default: \code{c(PIS = 0.30, BIS = 0.10, KDS = 0.15, SS = 0.10, PFS = 0.15, RS = 0.20)}.}
}
\value{
A named list with:
\describe{
\item{sii}{Numeric. Composite SII score (0-100).}
\item{dimensions}{A tibble with one row per dimension: name, label_fr,
label_en, score, weight, weighted_score, n_items.}
\item{data}{The input tibble with all individual scores added.}
\item{metadata}{A list with focal_author, total_items, career_span,
computation_date.}
}
}
\description{
Aggregates dimension-specific scores into a single composite index
on a 0-100 scale. The SII captures six dimensions of academic influence:
}
\details{
\enumerate{
\item \strong{Publication Impact} (PIS): journal prestige, citations, author position
\item \strong{Book Impact} (BIS): publisher prestige, citations
\item \strong{Knowledge Dissemination} (KDS): seminars, conferences, geographic scope
\item \strong{Supervision} (SS): thesis direction, student count
\item \strong{Research Funding} (PFS): project funding, role, funder prestige
\item \strong{Academic Recognition} (RS): awards, expertise, advisory roles
}
}
\examples{
\dontrun{
bib <- pg_read_bib(system.file("extdata", "duflo_articles.bib",
                                package = "publigraphics"))
extra <- pg_read_extra(system.file("extdata", "duflo_extra.csv",
                                   package = "publigraphics"))
merged <- pg_merge_inputs(bib, extra) |> pg_classify()
result <- pg_compute_sii(merged, focal_author = "Duflo")
result$sii
result$dimensions
}

}
