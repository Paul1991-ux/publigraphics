% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_narratives.R
\name{pg_check_api_key}
\alias{pg_check_api_key}
\title{Check Validity of an Anthropic API Key}
\usage{
pg_check_api_key(api_key)
}
\arguments{
\item{api_key}{Character. The Anthropic API key to validate.}
}
\value{
Logical. \code{TRUE} if the key appears valid (HTTP 200 or 400, which
indicates the key was accepted but the request body was incomplete),
\code{FALSE} if the key is rejected (HTTP 401). Returns \code{FALSE} with a
warning for any other HTTP status or network error.
}
\description{
Performs a minimal, low-cost call to the Anthropic Messages API endpoint
(\verb{/v1/messages}) to verify whether the supplied API key is valid. The call
requests only 1 token so that virtually no quota is consumed.
}
\details{
\strong{Security}: the API key is never logged, printed, or included in any
message. Only the HTTP status code is inspected.
}
\examples{
\dontrun{
valid <- pg_check_api_key(Sys.getenv("ANTHROPIC_API_KEY"))
if (valid) message("API key is valid")
}

}
