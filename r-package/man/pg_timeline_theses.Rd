% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_other.R
\name{pg_timeline_theses}
\alias{pg_timeline_theses}
\title{Vertical Timeline of Supervised Theses}
\usage{
pg_timeline_theses(data, theme_color = "#1B4F72")
}
\arguments{
\item{data}{A data frame containing at least \code{type_classified}, \code{title},
and \code{year} columns.}

\item{theme_color}{Character. Base hex colour for PhD points
(default \code{"#1B4F72"}). Master points use this colour at 50\%
opacity.}
}
\value{
A \code{ggplot} object.
}
\description{
Produces a vertical timeline of supervised theses (PhD and Master),
with points coloured by degree level and labels showing the thesis title.
}
\details{
The degree level is inferred from the \code{note} or \code{type_raw} field. If the
string contains "phd", "doctorat", or "these/these", it is classified as PhD;
if it contains "master", "m2", "m1", or "memoire", it is classified as
Master. Otherwise it defaults to "Other".
}
\examples{
\dontrun{
bib   <- pg_read_bib("references.bib")
extra <- pg_read_extra("extra.csv")
all   <- pg_merge_inputs(bib, extra) |> pg_classify()
pg_timeline_theses(all)
}

}
