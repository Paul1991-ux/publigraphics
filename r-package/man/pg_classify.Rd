% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_outputs.R
\name{pg_classify}
\alias{pg_classify}
\title{Classify research productions into canonical types}
\usage{
pg_classify(data)
}
\arguments{
\item{data}{A tibble produced by \code{\link[=pg_merge_inputs]{pg_merge_inputs()}}. Must contain at least
the column \code{type_raw} (character). The optional discriminant columns
\code{type}, \code{venue}, and \code{role} are used when present to refine
classification.}
}
\value{
The same tibble with column \code{type_classified} filled in. Rows that
cannot be mapped to one of the 12 canonical categories receive the value
\code{"other"} and a bilingual warning is emitted.
}
\description{
Assigns one of 12 canonical types (plus \code{"other"}) to each row of a
merged-input tibble produced by \code{\link[=pg_merge_inputs]{pg_merge_inputs()}}. Classification relies
on the BibTeX entry type stored in \code{type_raw} and, where necessary, on
discriminant fields such as \code{type}, \code{venue}, and \code{role}.
}
\details{
\subsection{Classification rules}{\tabular{ll}{
   \code{type_classified} \tab Source entry types / conditions \cr
   \code{article} \tab \verb{@article} \cr
   \code{book} \tab \verb{@book} \cr
   \code{book_chapter} \tab \verb{@incollection}, \verb{@inbook} \cr
   \code{seminar} \tab \verb{@inproceedings}/\verb{@conference} with "seminar" in type/venue \cr
   \code{conference} \tab \verb{@inproceedings}/\verb{@conference} (other) \cr
   \code{report} \tab \verb{@techreport}, \verb{@report} \cr
   \code{thesis_supervised} \tab \verb{@phdthesis}/\verb{@mastersthesis} with \code{role == "supervisor"} \cr
   \code{patent} \tab \verb{@patent}, or \verb{@misc} with \code{type == "patent"} \cr
   \code{media} \tab \verb{@misc} with \code{type} in \code{"media"}, \code{"podcast"}, \code{"interview"} \cr
   \code{project} \tab \verb{@misc}/CSV with \code{type} in \code{"project"}, \code{"grant"} \cr
   \code{award} \tab \verb{@misc}/CSV with \code{type} in \code{"award"}, \code{"prize"} \cr
   \code{expertise} \tab \verb{@misc}/CSV with \code{type} in \code{"expertise"}, \code{"consulting"} \cr
   \code{other} \tab Everything else (warning emitted) \cr
}

}
}
\examples{
# Build a small demo tibble that mimics pg_merge_inputs() output
demo <- tibble::tibble(
  id        = paste0("ref_", 1:6),
  type_raw  = c("article", "book", "incollection",
                 "inproceedings", "misc", "misc"),
  type      = c(NA, NA, NA, "seminar", "media", "project"),
  venue     = c(NA, NA, NA, "Seminar on Trade", NA, NA),
  role      = c(NA, NA, NA, NA, NA, NA),
  title     = paste("Title", 1:6),
  year      = c(2019, 2020, 2021, 2022, 2023, 2024),
  type_classified = NA_character_
)

classified <- pg_classify(demo)
classified$type_classified

}
