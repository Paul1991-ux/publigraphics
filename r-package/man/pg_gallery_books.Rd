% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_books.R
\name{pg_gallery_books}
\alias{pg_gallery_books}
\title{Gallery of Book Covers from Open Library}
\usage{
pg_gallery_books(data, theme_color = "#1B4F72")
}
\arguments{
\item{data}{A data frame (typically from \code{\link[=pg_read_bib]{pg_read_bib()}} followed by
\code{\link[=pg_classify]{pg_classify()}}) containing at least \code{type_classified}, \code{title}, and
\code{isbn} columns.}

\item{theme_color}{Character. Base hex colour for placeholder backgrounds
and theme accents (default \code{"#1B4F72"}).}
}
\value{
A \code{patchwork} ggplot object assembling book cover panels.
Returns an empty \code{ggplot() + theme_void()} when no books are found.
}
\description{
Fetches book cover images from the Open Library Covers API using ISBNs found
in the data. When a cover cannot be retrieved (HTTP error or missing ISBN), a
placeholder is generated with \link[ggplot2:ggplot2-package]{ggplot2::ggplot2}. The individual panels are assembled
into a grid via \code{\link[patchwork:wrap_plots]{patchwork::wrap_plots()}}.
}
\examples{
\dontrun{
bib  <- pg_read_bib("references.bib") |> pg_classify()
gallery <- pg_gallery_books(bib)
print(gallery)
}

}
