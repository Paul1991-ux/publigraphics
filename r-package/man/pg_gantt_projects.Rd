% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_other.R
\name{pg_gantt_projects}
\alias{pg_gantt_projects}
\title{Gantt Chart of Funded Projects}
\usage{
pg_gantt_projects(data, theme_color = "#1B4F72")
}
\arguments{
\item{data}{A data frame (typically from \code{\link[=pg_merge_inputs]{pg_merge_inputs()}} and
\code{\link[=pg_classify]{pg_classify()}}) containing at least \code{type_classified}, \code{title}, \code{note},
and \code{year} columns.}

\item{theme_color}{Character. Base hex colour for ongoing projects and
theme accents (default \code{"#1B4F72"}).}
}
\value{
A \code{ggplot} object.
}
\description{
Builds a horizontal Gantt chart showing the duration of funded projects.
Bars are coloured by project status (completed, ongoing, future) and
annotated with the funding source when available.
}
\details{
Date information is extracted from the \code{note} field, which is expected to
contain pipe-separated metadata including ISO-formatted \code{date_start} and
\code{date_end} values (e.g. \code{"Funding: 50000 | Source: ANR | 2020-01-15 | 2023-06-30"}).
If explicit dates are not found, the function falls back to the \code{year}
column to construct approximate date ranges.
}
\examples{
\dontrun{
bib   <- pg_read_bib("references.bib")
extra <- pg_read_extra("extra.csv")
all   <- pg_merge_inputs(bib, extra) |> pg_classify()
pg_gantt_projects(all)
}

}
