% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_inputs.R
\name{pg_read_extra}
\alias{pg_read_extra}
\title{Read a supplementary CSV file for non-bibliographic items}
\usage{
pg_read_extra(path, encoding = "UTF-8")
}
\arguments{
\item{path}{Character. Path to the supplementary CSV file.}

\item{encoding}{Character. File encoding (default \code{"UTF-8"}).}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with the 19-column publigraphics schema.
Column \code{source} is set to \code{"extra"} for all rows.
}
\description{
Reads a CSV file containing supplementary research outputs such as
seminars, funded projects, supervised theses, and awards that are not
captured in standard bibliographic databases. The data are mapped to
the canonical publigraphics schema with \code{source = "extra"}.
}
\details{
The CSV file is expected to contain some or all of the following columns
(case insensitive): \code{type}, \code{title}, \code{year}, \code{city}, \code{country},
\code{institution}, \code{note}, \code{date_start}, \code{date_end}, \code{funding_amount},
\code{funding_source}. Missing columns default to \code{NA}.

The \code{type} column is stored in \code{type_raw}; the \code{funding_amount} and
\code{funding_source} columns are concatenated into the \code{note} field if
present.
}
\examples{
# Using the bundled demo extra CSV
extra_path <- system.file("extdata", "demo_extra.csv",
                          package = "publigraphics")
if (nzchar(extra_path)) {
  extras <- pg_read_extra(extra_path)
  print(extras)
}

}
