% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_other.R
\name{pg_infographic_awards}
\alias{pg_infographic_awards}
\title{Infographic Display of Awards and Distinctions}
\usage{
pg_infographic_awards(data, theme_color = "#1B4F72")
}
\arguments{
\item{data}{A data frame containing at least \code{type_classified}, \code{title},
and \code{year} columns. The \code{institution} column is used when available.}

\item{theme_color}{Character. Base hex colour for accents and alternating
row tinting (default \code{"#1B4F72"}).}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a vertical "palmares" infographic using \code{ggplot2}, displaying each
award as a tile with a medal icon (Unicode), the award title, granting
institution, and year. Alternating rows use a subtle background tint.
}
\examples{
\dontrun{
bib   <- pg_read_bib("references.bib")
extra <- pg_read_extra("extra.csv")
all   <- pg_merge_inputs(bib, extra) |> pg_classify()
pg_infographic_awards(all)
}

}
