% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_articles.R
\name{pg_card_article}
\alias{pg_card_article}
\title{HTML Card for One Article with Narrative Summary}
\usage{
pg_card_article(article_row, narrative, theme_color = "#1B4F72", lang = "fr")
}
\arguments{
\item{article_row}{A one-row data frame with article metadata. Expected
fields: \code{title}, \code{author}, \code{journal}, \code{year}, \code{doi}.}

\item{narrative}{A one-row \code{\link[tibble:tibble]{tibble::tibble()}} as returned by
\code{\link[=pg_narrative_article]{pg_narrative_article()}}, with columns \code{problematique}, \code{pertinence},
\code{resultat}, \code{question_ouverte}.}

\item{theme_color}{Character. Hex colour for the left border and header
badge (default \code{"#1B4F72"}).}

\item{lang}{Character. Display language: \code{"fr"} (default) or \code{"en"}.}
}
\value{
A \code{character(1)} HTML string suitable for inclusion in R Markdown /
Quarto documents via \code{\link[htmltools:HTML]{htmltools::HTML()}}.
}
\description{
Generates an HTML card for a single article that includes bibliographic
metadata (title, authors, journal, DOI) and the four narrative sections
produced by \code{\link[=pg_narrative_article]{pg_narrative_article()}}.
}
\examples{
\dontrun{
article   <- bib[1, ]
narrative <- pg_narrative_article(article, api_key = Sys.getenv("ANTHROPIC_API_KEY"))
html_card <- pg_card_article(article, narrative)
htmltools::browsable(htmltools::HTML(html_card))
}

}
