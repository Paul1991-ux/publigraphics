% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_seminars.R
\name{pg_map_seminars}
\alias{pg_map_seminars}
\title{Geographic Map of Seminar and Conference Interventions}
\usage{
pg_map_seminars(data, output_type = "both", theme_color = "#1B4F72")
}
\arguments{
\item{data}{A data frame (typically from \code{\link[=pg_classify]{pg_classify()}}) containing at
least \code{type_classified}, \code{title}, \code{city}, and \code{country} columns.}

\item{output_type}{Character. Type of output to generate: \code{"both"}
(default), \code{"interactive"}, or \code{"static"}.}

\item{theme_color}{Character. Base hex colour for markers and points
(default \code{"#1B4F72"}).}
}
\value{
A named list with two elements:
\describe{
\item{interactive}{A \code{leaflet} htmlwidget (or \code{NULL} if
\code{output_type = "static"}).}
\item{static}{A \code{ggplot} object (or \code{NULL} if
\code{output_type = "interactive"}).}
}
}
\description{
Creates an interactive Leaflet map and/or a static ggplot2 map showing
the geographic distribution of seminars and conferences. Each location
is represented by a proportional circle whose radius encodes the number
of interventions.
}
\details{
Locations are geocoded using \code{\link[tidygeocoder:geo]{tidygeocoder::geo()}} with the
OpenStreetMap (\code{"osm"}) method. Results are cached in a temporary file
for the duration of the R session to avoid repeated API calls.

The \strong{interactive} version uses \code{\link[leaflet:leaflet]{leaflet::leaflet()}} with
\code{CartoDB.Positron} tiles and circle markers with HTML popups listing
intervention titles.

The \strong{static} version uses \code{\link[rnaturalearth:ne_countries]{rnaturalearth::ne_countries()}} for the
basemap and \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}} for proportional symbols, styled
with \code{\link[=pg_theme]{pg_theme()}}.
}
\examples{
\dontrun{
bib   <- pg_read_bib("references.bib")
extra <- pg_read_extra("extra.csv")
data  <- pg_merge_inputs(bib, extra) |> pg_classify()
maps  <- pg_map_seminars(data)
maps$interactive         # open in viewer
print(maps$static)       # static PDF-ready
}

}
